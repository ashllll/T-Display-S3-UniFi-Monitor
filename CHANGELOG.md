## 2026-09-16 — HTTP 恢复、长时间运行与文档更新

- HTTP 设置/传输失败、网络变化和不完整响应后清理客户端，确保下次重试发送新 GET；响应头 EAGAIN 归类为超时。
- 保留正常连接复用、TLS 验证、认证状态和 429 冷却；增加实际 HTTP 包装函数的主机回归测试。
- UI 使用 64 位毫秒时钟，避免长时间运行回绕；旧 Ping 会话回调不能写入新网络历史。
- 更新五页界面、接口路径、速率单位、刷新/过期规则、配置与实机验收流程。
- 恢复/时序/解析测试与固件构建通过，实机烧录校验和启动采集通过；真实断网恢复与本轮相机验收仍待验证。
